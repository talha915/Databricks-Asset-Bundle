resources:
  pipelines:
    bronze_pipeline:

      name: bronze_pipeline

      catalog: ai_lab_demo
      schema: bronze
      serverless: true
      continuous: false

      libraries:
        - notebook:
            path: ../../src/notebooks/bronze.ipynb